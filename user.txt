# txt file to add a user define variable to bash.bashcr
# display ipv4 address of user
myip=$(ifconfig | grep -oP 'inet (\d+\.){3}\d+' | awk '{print $2}')
echo " 
   < ━━━━━━━━━━━━━━━ [★] $myip [★] ━━━━━━━━━━━━━ > " |lolcat
echo 
tput cnorm
